# Offline-only ML dependencies — NOT installed on Vercel.
#
# Needed to run website/precompute_forecasts.py, which runs the live XGBoost
# inference pipeline and writes the result to Firestore. Install into the
# local venv:  pip install -r requirements.txt -r requirements-ml.txt
numpy>=1.26
pandas>=2.0
scikit-learn>=1.4
xgboost>=2.0
joblib>=1.3
